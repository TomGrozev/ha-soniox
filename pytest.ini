[pytest]
asyncio_mode = auto
testpaths = tests
# Root on sys.path so tests can import custom_components.soniox, and so the
# enable_custom_integrations loader picks the component up (run from root).
pythonpath = .
# Keep the tree lean: only tests/ is scanned for test modules.
norecursedirs = .git .venv custom_components docs specs
